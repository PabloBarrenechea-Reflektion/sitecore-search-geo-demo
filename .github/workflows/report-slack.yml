  name: Post to a Slack channel
  on:
    pull_request:
      types: [ closed ]
      branches:
        - main
  jobs:
    post-slack:
      runs-on: ubuntu-latest
      steps:
        - name: Prepare description
          id: prepare_description
          uses: actions/github-script@v3
          with:
            script: |
              let description = `${{ github.event.pull_request.body }}`;
              description = description.replace(/##/g, '').replace("Features", "*Features*").replace("Fixes", "*Fixes*");
              return description.replace(/#(\d+)/g, "<https://github.com/Reflektion/rfk-sdk/issues/$1|#$1>");
        - name: Send Slack notification
          uses: slackapi/slack-github-action@v1.24.0
          with:
            channel-id: '#sdk-notifications,#widget-sdk-private'
            payload: |
              {
                "text": "*${{github.event.pull_request.title}}*",
                
                "blocks": [
                  {
                    "type": "section",
                    "text": {
                      "type": "mrkdwn",
                      "text": "*${{github.event.pull_request.title}}*"
                    }
                  },
                  {"type" : "divider"},
                  {
                    "type": "section",
                    "text": {
                      "type": "mrkdwn",
                      "text": ${{ steps.prepare_description.outputs.result }}
                    }
                  }
                ]
              }
          env:
            SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
